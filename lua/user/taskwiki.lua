-- this is only for Vimwiki 
--    * should make this a branch, because doesn't work out-of-box
--    * do we want to mod it?  To work with Wiki.vim ?

local M = {
  "tools-life/taskwiki",
  dependencies = {
    "powerman/vim-plugin-AnsiEsc",
  },
}

function M.config()
   -- do nothing
end

return M
